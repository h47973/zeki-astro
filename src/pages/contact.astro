---
import BaseLayout from '../layouts/BaseLayout.astro';

const offices = [
  {
    country: "United States",
    flag: "fi fi-ss-flag",
    address: "1049 El Monte Avenue Ste C #684",
    city: "Mountain View, CA 94040",
    phone: "+1 (202) 992-5235",
    email: "usa@zekiexperts.com",
    timezone: "PST (UTC-8)",
    coords: { lat: 37.3861, lng: -122.0839, name: "Mountain View" },
    color: "#3b82f6"
  },
  {
    country: "Pakistan",
    flag: "fi fi-ss-flag", 
    address: "Kickstart Building, 189 Korang Road, I-10/3",
    city: "Islamabad, Islamabad Capital Territory",
    phone: "+92 331 1110214",
    email: "pk@zekiexperts.com",
    timezone: "PKT (UTC+5)",
    coords: { lat: 33.6844, lng: 73.0479, name: "Islamabad" },
    color: "#06b6d4"
  }
];

const faqs = [
  {
    question: "How quickly can you match me with developers?",
    answer: "We typically match clients with suitable developers within 24-48 hours after understanding your requirements."
  },
  {
    question: "What is your vetting process for developers?",
    answer: "Our developers undergo technical assessments, portfolio reviews, and interviews to ensure they meet our quality standards."
  },
  {
    question: "Do you offer project management services?",
    answer: "Yes, we provide optional project management services to help coordinate development work and ensure timely delivery."
  },
  {
    question: "What technologies do your developers specialize in?",
    answer: "We specialize in modern web and mobile technologies including React, Node.js, Python, React Native, Flutter, AWS, and more. Our team stays up-to-date with the latest industry trends."
  }
];
---

<BaseLayout title="Contact Zeki Experts - Get Expert Developers in 24 Hours">
  <!-- Hero Section with Advanced Animations -->
  <section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-gray-50 via-white to-blue-50 dark:from-gray-900 dark:via-gray-800 dark:to-blue-900">
    <!-- Particle Canvas -->
    <div class="absolute inset-0">
      <canvas id="contact-particles" class="w-full h-full"></canvas>
    </div>
    
    <!-- Floating Tech Shapes -->
    <div class="absolute inset-0">
      <div class="floating-tech tech-1"></div>
      <div class="floating-tech tech-2"></div>
      <div class="floating-tech tech-3"></div>
      <div class="floating-tech tech-4"></div>
    </div>
    
    <!-- Morphing Background Shapes -->
    <div class="absolute inset-0">
      <div class="morph-shape morph-1"></div>
      <div class="morph-shape morph-2"></div>
    </div>
    
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <!-- Animated Badge -->
      <div class="animated-badge mb-8">
        <span class="pulse-dot"></span>
        Expert Developers in 24 Hours
      </div>
      
      <!-- Main Title with Staggered Animation -->
      <h1 class="hero-title mb-6">
        <span class="title-line">Connect with</span>
        <span class="title-line gradient-text">Top Developers</span>
      </h1>
      
      <!-- Enhanced Subtitle -->
      <p class="hero-subtitle mb-12">
        Get matched with vetted software developers, designers, and technical experts
        who can start working on your project immediately.
      </p>
      
      <!-- CTA Buttons -->
      <div class="cta-buttons">
        <a href="#contact-form" class="cta-primary">
          <span>Get Started Today</span>
          <div class="button-shine"></div>
        </a>
        
        <div class="contact-info">
          <span>Call us: </span>
          <a href="tel:+12029925235" class="phone-link">+1 (202) 992-5235</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-20 bg-white dark:bg-gray-900 relative overflow-hidden">
    <!-- Background Pattern -->
    <div class="absolute inset-0 opacity-5">
      <div class="animated-grid"></div>
    </div>
    
    <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="section-title mb-6">Why Choose Zeki Experts?</h2>
        <div class="title-underline mx-auto"></div>
        <p class="section-subtitle mt-6">
          Trusted by businesses worldwide for our expertise and reliability
        </p>
      </div>
      
      <div class="stats-grid">
        <div class="stat-card card-tinted" data-aos="zoom-in" data-aos-delay="0">
          <div class="stat-icon"><i class="fi fi-ss-bolt"></i></div>
          <div class="stat-number">24h</div>
          <div class="stat-label">Average Match Time</div>
        </div>
        
        <div class="stat-card card-tinted" data-aos="zoom-in" data-aos-delay="100">
          <div class="stat-icon">👥</div>
          <div class="stat-number">500+</div>
          <div class="stat-label">Expert Developers</div>
        </div>
        
        <div class="stat-card card-tinted" data-aos="zoom-in" data-aos-delay="200">
          <div class="stat-icon"><i class="fi fi-ss-target"></i></div>
          <div class="stat-number">98%</div>
          <div class="stat-label">Success Rate</div>
        </div>
        
        <div class="stat-card card-tinted" data-aos="zoom-in" data-aos-delay="300">
          <div class="stat-icon">🌍</div>
          <div class="stat-number">50+</div>
          <div class="stat-label">Countries Served</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Offices Section -->
  <section class="py-32 bg-white dark:bg-gray-900 relative overflow-hidden">
    <!-- Morphing Background -->
    <div class="absolute inset-0">
      <div class="morph-shape morph-office-1"></div>
      <div class="morph-shape morph-office-2"></div>
    </div>
    
    <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-20">
        <h2 class="section-title mb-6">Our Global Offices</h2>
        <div class="title-underline mx-auto"></div>
        <p class="section-subtitle mt-6">
          Strategically located offices to serve clients across different time zones
        </p>
      </div>
      
      <div class="flex justify-center">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl">
          {offices.map((office, index) => (
            <div class="office-card card-tinted">
            
            <div class="relative">
              <!-- Flag Icon -->
              <div class="office-flag"><i class={office.flag}></i></div>
              
              <!-- Country Name -->
              <h3 class="office-country">{office.country}</h3>
              
              <!-- Address Info -->
              <div class="office-address">
                <p>{office.address}</p>
                <p>{office.city}</p>
              </div>
              
              <!-- Contact Info -->
              <div class="office-contact">
                <a href={`tel:${office.phone}`} class="contact-link phone">
                  <span class="contact-icon"><i class="fi fi-ss-phone-call"></i></span>
                  {office.phone}
                </a>
                <a href={`mailto:${office.email}`} class="contact-link email">
                  <span class="contact-icon"><i class="fi fi-ss-envelope"></i></span>
                  {office.email}
                </a>
              </div>
              
              <!-- Timezone Badge -->
              <div class="timezone-badge">
                <span class="timezone-icon">🕐</span>
                <span>{office.timezone}</span>
              </div>
              
              <!-- Glow Effect -->
              <div class="office-glow"></div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ Section with Animations -->
  <section class="py-32 bg-white dark:bg-gray-900">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-20">
        <h2 class="section-title mb-6">Frequently Asked Questions</h2>
        <div class="title-underline mx-auto"></div>
        <p class="section-subtitle mt-6">
          Everything you need to know about working with our expert developers
        </p>
      </div>
      
      <div class="faq-container">
        {faqs.map((faq, index) => (
          <div class="faq-item card-tinted" data-aos="fade-up" data-aos-delay={(index * 100).toString()}>
            <details class="faq-details group">
              <summary class="faq-question">
                <h3 class="question-text">{faq.question}</h3>
                <div class="question-icon">
                  <svg class="chevron" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </div>
              </summary>
              <div class="faq-answer">
                <p>{faq.answer}</p>
              </div>
            </details>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section with Advanced Animations -->
  <section class="py-32 bg-gradient-to-r from-blue-600 via-purple-600 to-cyan-600 relative overflow-hidden animated-gradient">
    <!-- Animated Background Pattern -->
    <div class="absolute inset-0 opacity-20">
      <div class="animated-pattern"></div>
    </div>
    
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="cta-title mb-8">
        Ready to Find Your Perfect Developer?
      </h2>
      <p class="cta-subtitle mb-12">
        Join thousands of businesses who trust Zeki Experts for their development needs.
        Get matched with the right talent in just 24 hours.
      </p>
      
      <div class="cta-buttons">
        <a href="#contact-form" class="cta-primary">
          <span>Start Your Project Today</span>
          <div class="button-shine"></div>
        </a>
        
        <div class="contact-info">
          <span>Call us: </span>
          <a href="tel:+12029925235" class="phone-link">+1 (202) 992-5235</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Advanced Contact Form Section -->
  <section id="contact-form" class="py-32 bg-gray-50 dark:bg-gray-800 relative overflow-hidden">
    <!-- Grid Background -->
    <div class="absolute inset-0 opacity-5">
      <div class="grid-pattern"></div>
    </div>
    
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="section-title mb-6">Let's Start Your Project</h2>
        <div class="title-underline mx-auto"></div>
        <p class="section-subtitle mt-6">
          Tell us about your project requirements and we'll match you with the perfect developers
        </p>
      </div>
      
      <div class="form-container">
        <form class="contact-form" action="/api/contact" method="POST">
          <div class="form-grid">
            <div class="form-group">
              <label for="name" class="form-label">Full Name</label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="form-input"
                placeholder="Your full name"
              />
            </div>
            
            <div class="form-group">
              <label for="email" class="form-label">Email Address</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="form-input"
                placeholder="your.email@company.com"
              />
            </div>
            
            <div class="form-group">
              <label for="company" class="form-label">Company Name</label>
              <input
                type="text"
                id="company"
                name="company"
                class="form-input"
                placeholder="Your company name"
              />
            </div>
            
            <div class="form-group">
              <label for="phone" class="form-label">Phone Number</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="form-input"
                placeholder="+1 (555) 123-4567"
              />
            </div>
          </div>
          
          <div class="form-group">
            <label for="project" class="form-label">Project Description</label>
            <textarea
              id="project"
              name="project"
              required
              rows="6"
              class="form-input"
              placeholder="Tell us about your project requirements, technologies needed, timeline, and team size..."
            ></textarea>
          </div>
          
          <div class="form-submit-container">
            <button type="submit" class="form-submit">
              <span>Send Project Details</span>
              <svg class="submit-arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </button>
          </div>
        </form>
      </div>
    </div>
  </section>

  <!-- Contact Methods Grid -->
  <section class="py-24 bg-white dark:bg-gray-900">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="contact-methods-grid">
        <div class="contact-method" data-aos="fade-up" data-aos-delay="0">
          <div class="contact-method-icon"><i class="fi fi-ss-phone-call"></i></div>
          <h3 class="contact-method-title">Call Us</h3>
          <p class="contact-method-description">
            Speak directly with our team about your project requirements
          </p>
          <a href="tel:+12029925235" class="contact-method-link">+1 (202) 992-5235</a>
        </div>
        
        <div class="contact-method" data-aos="fade-up" data-aos-delay="100">
          <div class="contact-method-icon"><i class="fi fi-ss-envelope"></i></div>
          <h3 class="contact-method-title">Email Us</h3>
          <p class="contact-method-description">
            Send us detailed project requirements and we'll respond within 2 hours
          </p>
          <a href="mailto:hello@zekiexperts.com" class="contact-method-link">hello@zekiexperts.com</a>
        </div>
        
        <div class="contact-method" data-aos="fade-up" data-aos-delay="200">
          <div class="contact-method-icon"><i class="fi fi-ss-comment"></i></div>
          <h3 class="contact-method-title">Live Chat</h3>
          <p class="contact-method-description">
            Get instant answers to your questions from our support team
          </p>
          <button class="contact-method-link chat-trigger">Start Chat</button>
        </div>
      </div>
    </div>
  </section>

  <!-- Advanced Styles and Animations -->
  <style>
    /* Floating Tech Icon Shapes */
    .floating-tech {
      position: absolute;
      background: linear-gradient(45deg, rgba(59, 130, 246, 0.15), rgba(147, 51, 234, 0.15));
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: 16px;
      animation: float-contact 32s infinite ease-in-out;
      backdrop-filter: blur(15px);
    }
    
    .tech-1 { width: 90px; height: 90px; top: 8%; left: 14%; animation-delay: 0s; }
    .tech-2 { width: 70px; height: 70px; top: 18%; right: 22%; animation-delay: 8s; }
    .tech-3 { width: 80px; height: 80px; top: 58%; left: 20%; animation-delay: 16s; }
    .tech-4 { width: 75px; height: 75px; top: 68%; right: 28%; animation-delay: 24s; }
    
    @keyframes float-contact {
      0%, 100% { transform: translate(0px, 0px) rotate(0deg) scale(1); opacity: 0.4; }
      25% { transform: translate(18px, -28px) rotate(3deg) scale(1.1); opacity: 0.7; }
      50% { transform: translate(-22px, 32px) rotate(-3deg) scale(0.9); opacity: 0.3; }
      75% { transform: translate(28px, 18px) rotate(2deg) scale(1.05); opacity: 0.8; }
    }
    
    /* Morphing Background Shapes */
    .morph-shape {
      position: absolute;
      border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
      animation: morph-contact 29s ease-in-out infinite;
      opacity: 0.12;
    }
    
    .dark .morph-shape {
      opacity: 0.1;
    }
    
    .morph-1 {
      width: 420px;
      height: 420px;
      background: linear-gradient(45deg, #3b82f6, #8b5cf6);
      top: 3%;
      left: 1%;
      animation-delay: 0s;
    }
    
    .morph-2 {
      width: 340px;
      height: 340px;
      background: linear-gradient(45deg, #8b5cf6, #06b6d4);
      bottom: 8%;
      right: 3%;
      animation-delay: 14s;
    }
    
    .morph-office-1 {
      width: 380px;
      height: 380px;
      background: linear-gradient(45deg, #06b6d4, #3b82f6);
      top: 12%;
      left: 2%;
      animation-delay: 8s;
    }
    
    .morph-office-2 {
      width: 400px;
      height: 400px;
      background: linear-gradient(45deg, #8b5cf6, #06b6d4);
      bottom: 12%;
      right: 6%;
      animation-delay: 22s;
    }
    
    @keyframes morph-contact {
      0%, 100% { 
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
        transform: translate(0px, 0px) rotate(0deg);
      }
      50% { 
        border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%;
        transform: translate(55px, -55px) rotate(180deg);
      }
    }
    
    /* Animated Badge */
    .animated-badge {
      display: inline-flex;
      align-items: center;
      padding: 16px 32px;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 50px;
      color: #3b82f6;
      font-weight: 600;
      font-size: 0.95rem;
      animation: badge-float 3s ease-in-out infinite;
    }
    
    .dark .animated-badge {
      color: #60a5fa;
    }
    
    .pulse-dot {
      width: 10px;
      height: 10px;
      background: #3b82f6;
      border-radius: 50%;
      margin-right: 12px;
      animation: pulse-glow 2s ease-in-out infinite;
    }
    
    @keyframes badge-float {
      0%, 100% { transform: translateY(0px); box-shadow: 0 10px 30px rgba(59, 130, 246, 0.2); }
      50% { transform: translateY(-5px); box-shadow: 0 20px 40px rgba(59, 130, 246, 0.4); }
    }
    
    @keyframes pulse-glow {
      0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.7); }
      50% { transform: scale(1.1); box-shadow: 0 0 0 10px rgba(59, 130, 246, 0); }
    }
    
    /* Hero Title Animation */
    .hero-title {
      font-size: clamp(3rem, 8vw, 7rem);
      font-weight: 900;
      line-height: 1.1;
      color: #1f2937;
      text-align: center;
      overflow: hidden;
    }
    
    .dark .hero-title {
      color: #ffffff;
    }
    
    .dark .hero-title {
      color: #ffffff;
    }
    
    .title-line {
      display: block;
      animation: slide-up-stagger 1.2s cubic-bezier(0.23, 1, 0.32, 1);
    }
    
    .title-line:nth-child(2) {
      animation-delay: 0.2s;
    }
    
    .gradient-text {
      background: linear-gradient(45deg, #3b82f6, #8b5cf6, #06b6d4);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      background-size: 200% 200%;
      animation: gradient-shift 3s ease-in-out infinite;
    }
    
    @keyframes slide-up-stagger {
      0% { transform: translateY(100%); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }
    
    @keyframes gradient-shift {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    
    /* Hero Subtitle */
    .hero-subtitle {
      font-size: clamp(1.125rem, 3vw, 1.5rem);
      color: #6b7280;
      max-width: 800px;
      margin: 0 auto;
      line-height: 1.6;
      animation: fade-in-up 1.5s cubic-bezier(0.23, 1, 0.32, 1) 0.4s both;
    }
    
    .dark .hero-subtitle {
      color: #ffffff !important;
      font-weight: 500;
    }
    
    @keyframes fade-in-up {
      0% { transform: translateY(30px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }
    
    /* Section Styling */
    .section-title {
      font-size: clamp(2.5rem, 6vw, 4rem);
      font-weight: 900;
      color: #1f2937;
      text-align: center;
    }
    
    .dark .section-title {
      color: #ffffff;
    }
    
    .dark .section-title {
      color: #ffffff;
      text-shadow: 0 0 30px rgba(59, 130, 246, 0.5);
    }
    
    .section-subtitle {
      font-size: 1.25rem;
      color: #6b7280;
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
      line-height: 1.7;
    }
    
    .dark .section-subtitle {
      color: #ffffff !important;
      font-weight: 500;
    }
    
    .title-underline {
      width: 80px;
      height: 4px;
      background: linear-gradient(45deg, #3b82f6, #8b5cf6);
      border-radius: 2px;
      animation: expand-underline 2s cubic-bezier(0.23, 1, 0.32, 1);
    }
    
    @keyframes expand-underline {
      0% { width: 0; }
      100% { width: 80px; }
    }
    
    /* Stats Grid */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }
    
    .stat-card {
      text-align: center;
      padding: 2rem 1rem;
      background: linear-gradient(135deg, rgba(249, 115, 22, 0.1), rgba(239, 68, 68, 0.15));
      backdrop-filter: blur(20px);
      border: 1px solid rgba(249, 115, 22, 0.2);
      border-radius: 20px;
      transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
      animation: stat-bounce 2s cubic-bezier(0.68, -0.55, 0.265, 1.55);
      box-shadow: 0 8px 32px rgba(249, 115, 22, 0.1), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }
    
    .dark .stat-card {
      background: linear-gradient(135deg, rgba(249, 115, 22, 0.2), rgba(239, 68, 68, 0.25));
      backdrop-filter: blur(20px);
      border: 1px solid rgba(239, 68, 68, 0.3);
      box-shadow: 0 8px 32px rgba(239, 68, 68, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }
    
    .stat-card:hover {
      transform: translateY(-10px) scale(1.05);
      box-shadow: 0 20px 40px rgba(59, 130, 246, 0.4);
    }
    
    .stat-icon {
      font-size: 2rem;
    }
    
    .stat-number {
      font-size: 2.5rem;
      font-weight: 900;
      color: #1f2937;
    }
    
    .dark .stat-number {
      color: #ffffff;
    }
    
    .dark .stat-number {
      color: #ffffff;
    }
    
    .stat-label {
      font-size: 0.875rem;
      color: #6b7280;
      font-weight: 500;
    }
    
    .dark .stat-label {
      color: #d1d5db;
    }
    
    @keyframes stat-bounce {
      0% { transform: scale(0.3) rotate(-10deg); opacity: 0; }
      50% { transform: scale(1.1) rotate(5deg); }
      100% { transform: scale(1) rotate(0deg); opacity: 1; }
    }
    
    /* Form Container */
    .form-container {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(6, 182, 212, 0.15));
      backdrop-filter: blur(20px);
      border: 1px solid rgba(16, 185, 129, 0.2);
      border-radius: 30px;
      padding: 3rem;
      animation: form-float 8s ease-in-out infinite;
      box-shadow: 0 8px 32px rgba(16, 185, 129, 0.1), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }
    
    .dark .form-container {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(6, 182, 212, 0.25));
      border: 1px solid rgba(6, 182, 212, 0.3);
      box-shadow: 0 8px 32px rgba(6, 182, 212, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }
    
    @keyframes form-float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-5px); }
    }
    
    .contact-form {
      max-width: none;
      text-align: center;
    }
    
    .form-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-bottom: 0.5rem;
    }
    
    .form-group {
      text-align: left;
    }
    
    .form-label {
      display: block;
      color: #374151;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    
    .dark .form-label {
      color: #ffffff;
      text-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
    }
    
    .form-input {
      width: 100%;
      padding: 1rem 1.5rem;
      font-size: 0.95rem;
      border: 2px solid rgba(209, 213, 219, 0.3);
      border-radius: 15px;
      background: rgba(255, 255, 255, 0.5);
      backdrop-filter: blur(10px);
      color: #1f2937;
      transition: all 0.3s ease;
    }
    
    .dark .form-input {
      background: rgba(75, 85, 99, 0.3);
      border: 2px solid rgba(75, 85, 99, 0.5);
      color: #ffffff;
    }
    
    .form-input:focus {
      outline: none;
      border-color: #3b82f6;
      background: rgba(255, 255, 255, 0.8);
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(59, 130, 246, 0.2);
    }
    
    .form-input::placeholder {
      color: #9ca3af;
    }
    
    textarea.form-input {
      min-height: 120px;
      resize: vertical;
    }
    
    /* Form Submit */
    .form-submit-container {
      text-align: center;
      margin-top: 2rem;
    }
    
    .form-submit {
      position: relative;
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem 3rem;
      background: linear-gradient(45deg, #3b82f6, #8b5cf6);
      color: white;
      border: none;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1.1rem;
      cursor: pointer;
      transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
      overflow: hidden;
    }
    
    .form-submit:hover {
      transform: translateY(-5px) scale(1.05);
      box-shadow: 0 25px 50px rgba(59, 130, 246, 0.4);
    }
    
    .submit-arrow {
      width: 1.25rem;
      height: 1.25rem;
      transition: transform 0.3s ease;
    }
    
    .form-submit:hover .submit-arrow {
      transform: translateX(5px) rotate(15deg);
    }
    
    /* Contact Methods Grid */
    .contact-methods-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }
    
    .contact-method {
      text-align: center;
      padding: 2rem;
      background: linear-gradient(135deg, rgba(168, 85, 247, 0.1), rgba(236, 72, 153, 0.15));
      border: 1px solid rgba(168, 85, 247, 0.2);
      border-radius: 20px;
      transition: all 0.3s ease;
      backdrop-filter: blur(20px);
      box-shadow: 0 8px 32px rgba(168, 85, 247, 0.1), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }
    
    .dark .contact-method {
      background: linear-gradient(135deg, rgba(168, 85, 247, 0.2), rgba(236, 72, 153, 0.25));
      border: 1px solid rgba(236, 72, 153, 0.3);
      box-shadow: 0 8px 32px rgba(236, 72, 153, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }
    
    .contact-method:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
    }
    
    .contact-method-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }
    
    .contact-method-title {
      font-size: 1.25rem;
      font-weight: 700;
      color: #1f2937;
      margin-bottom: 1rem;
    }
    
    .dark .contact-method-title {
      color: #ffffff;
    }
    
    .dark .contact-method-title {
      color: #ffffff;
    }
    
    .contact-method-description {
      color: #6b7280;
      margin-bottom: 1.5rem;
      line-height: 1.6;
    }
    
    .dark .contact-method-description {
      color: #d1d5db;
    }
    
    .contact-method-link {
      display: inline-flex;
      align-items: center;
      padding: 0.75rem 1.5rem;
      background: #3b82f6;
      color: white;
      text-decoration: none;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      font-size: 0.95rem;
    }
    
    .contact-method-link:hover {
      background: #2563eb;
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(59, 130, 246, 0.3);
    }
    
    /* Office Cards */
    .office-card {
      position: relative;
      background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(147, 51, 234, 0.15));
      backdrop-filter: blur(20px);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: 20px;
      padding: 2.5rem 2rem;
      text-align: center;
      box-shadow: 0 8px 32px rgba(59, 130, 246, 0.1), inset 0 1px 0 rgba(255, 255, 255, 0.2);
      transition: all 0.3s ease;
    }
    
    .dark .office-card {
      background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(147, 51, 234, 0.25));
      border: 1px solid rgba(147, 51, 234, 0.3);
      box-shadow: 0 8px 32px rgba(147, 51, 234, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }
    
    .office-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 40px rgba(59, 130, 246, 0.15), inset 0 1px 0 rgba(255, 255, 255, 0.3);
    }

    .dark .office-card:hover {
      box-shadow: 0 20px 40px rgba(147, 51, 234, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }
    
    .office-flag {
      font-size: 4rem;
      margin-bottom: 1.5rem;
    }
    
    
    .office-country {
      font-size: 1.5rem;
      font-weight: 700;
      color: #1f2937;
      margin-bottom: 1.5rem;
    }
    
    .dark .office-country {
      color: #ffffff;
    }
    
    .dark .office-country {
      color: #ffffff;
      text-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
    }
    
    .office-address {
      color: #6b7280;
      margin-bottom: 1.5rem;
    }
    
    .dark .office-address {
      color: #d1d5db;
    }
    
    .office-contact {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin-bottom: 1.5rem;
    }
    
    .contact-link {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.75rem;
      background: #f3f4f6;
      color: #374151;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 500;
      font-size: 0.875rem;
      border: 1px solid #e5e7eb;
      transition: all 0.2s ease;
    }
    
    .contact-link:hover {
      background: #e5e7eb;
      color: #1f2937;
      border-color: #d1d5db;
    }

    .dark .contact-link {
      background: #374151;
      color: #e5e7eb;
      border: 1px solid #4b5563;
    }

    .dark .contact-link:hover {
      background: #4b5563;
      color: #ffffff;
      border-color: #6b7280;
    }
    
    .contact-icon {
      font-size: 1rem;
    }
    
    .timezone-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      background: rgba(34, 197, 94, 0.1);
      color: #22c55e;
      border-radius: 20px;
      font-size: 0.875rem;
      font-weight: 600;
    }
    
    .dark .timezone-badge {
      color: #4ade80;
    }
    
    .timezone-icon {
      font-size: 1rem;
    }
    
    .office-glow {
      position: absolute;
      top: -2px;
      left: -2px;
      right: -2px;
      bottom: -2px;
      background: linear-gradient(45deg, #3b82f6, #8b5cf6, #06b6d4);
      border-radius: 32px;
      opacity: 0;
      z-index: -1;
      transition: opacity 0.5s ease;
    }
    
    
    /* FAQ Styles */
    .faq-container {
      max-width: none;
      space-y: 1rem;
    }
    
    .faq-item {
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(251, 146, 60, 0.15));
      border: 1px solid rgba(245, 158, 11, 0.2);
      border-radius: 20px;
      overflow: hidden;
      transition: all 0.3s ease;
      backdrop-filter: blur(20px);
      box-shadow: 0 8px 32px rgba(245, 158, 11, 0.1), inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }
    
    .dark .faq-item {
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.2), rgba(251, 146, 60, 0.25));
      border: 1px solid rgba(251, 146, 60, 0.3);
      box-shadow: 0 8px 32px rgba(251, 146, 60, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }
    
    .faq-item:hover {
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }
    
    .faq-details[open] {
      background: rgba(59, 130, 246, 0.05);
    }
    
    .faq-question {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.5rem 2rem;
      cursor: pointer;
      list-style: none;
      transition: all 0.3s ease;
    }
    
    .faq-question:hover {
      background: rgba(59, 130, 246, 0.05);
    }
    
    .faq-details[open] .faq-question {
      background: rgba(59, 130, 246, 0.1);
    }
    
    .question-text {
      font-size: 1.1rem;
      font-weight: 600;
      color: #1f2937;
      margin: 0;
      flex: 1;
    }
    
    .dark .question-text {
      color: #ffffff;
    }
    
    .dark .question-text {
      color: #ffffff;
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
    }
    
    .question-icon {
      margin-left: 1rem;
    }
    
    .chevron {
      width: 1.25rem;
      height: 1.25rem;
      color: #6b7280;
      transition: transform 0.3s ease;
    }
    
    .dark .chevron {
      color: #d1d5db;
    }
    
    .faq-details[open] .chevron {
      transform: rotate(180deg);
    }
    
    .faq-answer {
      padding: 0 2rem 1.5rem;
      animation: fade-in-down 0.3s ease;
    }
    
    .faq-answer p {
      color: #6b7280;
      line-height: 1.7;
      margin: 0;
    }
    
    .dark .faq-answer p {
      color: #d1d5db;
    }
    
    @keyframes fade-in-down {
      0% { opacity: 0; transform: translateY(-10px); }
      100% { opacity: 1; transform: translateY(0); }
    }
    
    /* CTA Styles */
    .cta-primary {
      position: relative;
      display: inline-flex;
      align-items: center;
      padding: 1.25rem 3rem;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(20px);
      color: white;
      font-weight: 600;
      font-size: 1.1rem;
      border-radius: 50px;
      border: 1px solid rgba(255, 255, 255, 0.3);
      text-decoration: none;
      transition: all 0.3s ease;
      overflow: hidden;
    }
    
    .cta-primary:hover {
      background: rgba(255, 255, 255, 0.3);
      transform: translateY(-3px);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
    }
    
    .button-shine {
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
      transform: rotate(45deg);
      transition: all 0.6s ease;
      opacity: 0;
    }
    
    .cta-primary:hover .button-shine {
      opacity: 1;
      transform: rotate(45deg) translate(50%, 50%);
    }
    
    .cta-title {
      font-size: clamp(2.5rem, 6vw, 4rem);
      font-weight: 900;
      color: white;
      text-align: center;
    }
    
    .cta-subtitle {
      font-size: 1.25rem;
      color: rgba(255, 255, 255, 0.9);
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
      line-height: 1.6;
    }
    
    .cta-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 2rem;
    }
    
    @media (min-width: 640px) {
      .cta-buttons {
        flex-direction: row;
        justify-content: center;
      }
    }
    
    .contact-info {
      color: rgba(255, 255, 255, 0.9);
      font-size: 1rem;
    }
    
    .phone-link {
      color: white;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
    }
    
    .phone-link:hover {
      color: rgba(255, 255, 255, 0.8);
    }
    
    .animated-gradient {
      background-size: 400% 400%;
      animation: gradient-flow 8s ease infinite;
    }
    
    @keyframes gradient-flow {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    
    .animated-pattern {
      width: 100%;
      height: 100%;
      background-image: 
        radial-gradient(circle at 25% 25%, rgba(255, 255, 255, 0.2) 2px, transparent 2px),
        radial-gradient(circle at 75% 75%, rgba(255, 255, 255, 0.2) 2px, transparent 2px);
      background-size: 60px 60px;
      animation: pattern-move 20s linear infinite;
    }
    
    @keyframes pattern-move {
      0% { transform: translate(0, 0); }
      100% { transform: translate(60px, 60px); }
    }
    
    .animated-grid {
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(rgba(59, 130, 246, 0.1) 1px, transparent 1px),
        linear-gradient(90deg, rgba(59, 130, 246, 0.1) 1px, transparent 1px);
      background-size: 50px 50px;
      animation: grid-move 20s linear infinite;
    }
    
    @keyframes grid-move {
      0% { transform: translate(0, 0); }
      100% { transform: translate(50px, 50px); }
    }
    
    /* Accessibility */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
      }
    }
  </style>

  <!-- Advanced Particle System -->
  <script>
    class ContactParticleSystem {
      constructor(canvas) {
        if (!canvas) return;
        this.canvas = canvas;
        this.ctx = canvas.getContext('2d');
        if (!this.ctx) return;
        this.particles = [];
        this.particleCount = 40;
        this.mouse = { x: 0, y: 0 };
        this.init();
      }
      
      init() {
        this.resize();
        this.createParticles();
        this.bindEvents();
        this.animate();
      }
      
      resize() {
        this.canvas.width = this.canvas.offsetWidth;
        this.canvas.height = this.canvas.offsetHeight;
      }
      
      createParticles() {
        for (let i = 0; i < this.particleCount; i++) {
          this.particles.push({
            x: Math.random() * this.canvas.width,
            y: Math.random() * this.canvas.height,
            vx: (Math.random() - 0.5) * 0.4,
            vy: (Math.random() - 0.5) * 0.4,
            radius: Math.random() * 2.5 + 1,
            opacity: Math.random() * 0.6 + 0.2,
            color: (() => {
              const rand = Math.random();
              if (rand > 0.6) return '#3b82f6';
              if (rand > 0.3) return '#8b5cf6'; 
              return '#06b6d4';
            })()
          });
        }
      }
      
      bindEvents() {
        window.addEventListener('resize', () => this.resize());
        this.canvas.addEventListener('mousemove', (e) => {
          const rect = this.canvas.getBoundingClientRect();
          this.mouse.x = e.clientX - rect.left;
          this.mouse.y = e.clientY - rect.top;
        });
      }
      
      updateParticles() {
        this.particles.forEach(particle => {
          particle.x += particle.vx;
          particle.y += particle.vy;
          
          // Mouse interaction
          const dx = this.mouse.x - particle.x;
          const dy = this.mouse.y - particle.y;
          const distance = Math.sqrt(dx * dx + dy * dy);
          
          if (distance < 100) {
            const force = (100 - distance) / 100;
            particle.vx += dx * force * 0.0005;
            particle.vy += dy * force * 0.0005;
          }
          
          // Boundary bounce
          if (particle.x <= 0 || particle.x >= this.canvas.width) {
            particle.vx *= -1;
          }
          if (particle.y <= 0 || particle.y >= this.canvas.height) {
            particle.vy *= -1;
          }
          
          // Keep within bounds
          particle.x = Math.max(0, Math.min(this.canvas.width, particle.x));
          particle.y = Math.max(0, Math.min(this.canvas.height, particle.y));
          
          // Damping
          particle.vx *= 0.99;
          particle.vy *= 0.99;
        });
      }
      
      drawParticles() {
        this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
        
        // Draw connections
        this.particles.forEach((particle, i) => {
          this.particles.slice(i + 1).forEach(otherParticle => {
            const dx = particle.x - otherParticle.x;
            const dy = particle.y - otherParticle.y;
            const distance = Math.sqrt(dx * dx + dy * dy);
            
            if (distance < 150) {
              this.ctx.beginPath();
              this.ctx.strokeStyle = `rgba(59, 130, 246, ${(150 - distance) / 150 * 0.25})`;
              this.ctx.lineWidth = 1;
              this.ctx.moveTo(particle.x, particle.y);
              this.ctx.lineTo(otherParticle.x, otherParticle.y);
              this.ctx.stroke();
            }
          });
        });
        
        // Draw particles
        this.particles.forEach(particle => {
          this.ctx.beginPath();
          this.ctx.arc(particle.x, particle.y, particle.radius, 0, Math.PI * 2);
          this.ctx.fillStyle = particle.color;
          this.ctx.globalAlpha = particle.opacity;
          this.ctx.fill();
          this.ctx.globalAlpha = 1;
        });
      }
      
      animate() {
        this.updateParticles();
        this.drawParticles();
        requestAnimationFrame(() => this.animate());
      }
    }
    
    // Initialize when DOM is ready
    document.addEventListener('DOMContentLoaded', () => {
      try {
        const particleCanvas = document.getElementById('contact-particles');
        if (particleCanvas) {
          new ContactParticleSystem(particleCanvas);
        }
      } catch (error) {
        console.warn('Particle system initialization failed:', error);
        console.warn('Canvas not supported, animations disabled');
      }
      
      // Form enhancements
      const inputs = document.querySelectorAll('.form-input');
      if (inputs.length > 0) {
        inputs.forEach(input => {
          input.addEventListener('focus', function() {
            this.parentElement.classList.add('focused');
          });
          
          input.addEventListener('blur', function() {
            if (!this.value.trim()) {
              this.parentElement.classList.remove('focused');
            }
          });
        });
      }
      
      const form = document.querySelector('.contact-form');
      if (form) {
        form.addEventListener('submit', function(e) {
          const submitBtn = this.querySelector('.form-submit');
          submitBtn.innerHTML = '<span>Sending...</span><div class="loading-spinner"></div>';
          submitBtn.disabled = true;
        });
      }
    });
  </script>
</BaseLayout>